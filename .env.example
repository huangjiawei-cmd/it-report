# .env.example
# Optional: Specify a custom server outbound IP (egress IP) to bypass dynamic fetching
SERVER_IP=34.96.48.95

# Optional: Path to the persistent storage file for monthly reports and configurations
STORAGE_FILE_PATH=./report_storage.json
